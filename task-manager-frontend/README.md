# task-manager-frontend (starter)

Next.js app mínimo que consume la API .NET.

Quick start

1. From repo root:

```bash
cd task-manager-frontend
npm install
npm run dev
```

2. Asegúrate de que la API backend esté corriendo y ajusta BACKEND_URL si no está en localhost:5000.
